---
import SearchLayout from '../layouts/SearchLayout.astro';
---

<SearchLayout title="소장도서검색 - 연수구립도서관">
  <div class="search-demo-content">
    <h1>소장도서검색</h1>
    <p>좌측 메뉴에서 원하는 검색 카테고리를 선택해보세요.</p>
    
    <div class="search-form">
      <input type="text" placeholder="검색어를 입력하세요" />
      <button type="button">검색</button>
    </div>
  </div>
</SearchLayout>

<style lang="scss">
@use '../styles/abstracts' as *;

.search-demo-content {
  padding: to-rem(24);
  
  h1 {
    font-size: to-rem(24);
    margin-bottom: to-rem(16);
    color: $primary-color;
  }
  
  p {
    margin-bottom: to-rem(24);
    color: $gray-700;
  }
}

.search-form {
  display: flex;
  gap: to-rem(12);
  
  input {
    flex: 1;
    padding: to-rem(12);
    border: 1px solid $gray-300;
    border-radius: to-rem(4);
  }
  
  button {
    padding: to-rem(12) to-rem(24);
    background: $primary-color;
    color: white;
    border: none;
    border-radius: to-rem(4);
    cursor: pointer;
  }
}
</style>