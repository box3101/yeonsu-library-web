---
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import '../styles/main.scss';

// 전역 컴포넌트 import
import UiButton from '@ui/UiButton.astro';
import UiRadio from '@ui/UiRadio.astro';
import UiSelect from '@ui/UiSelect.astro';
import UiNavButton from '@ui/UiNavButton.astro';
import UiTab from '@ui/UiTab.astro';

// 전역 등록
if (typeof globalThis !== 'undefined') {
  globalThis.UiButton = UiButton;
  globalThis.UiRadio = UiRadio;
  globalThis.UiSelect = UiSelect;
  globalThis.UiNavButton = UiNavButton;
  globalThis.UiTab = UiTab;
}
---

<html>
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <!-- Pretendard 폰트 CDN -->
    <link rel="preconnect" href="https://cdn.jsdelivr.net" />
    <link
      rel="stylesheet"
      as="style"
      crossorigin
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css"
    />
    <!-- <link rel="stylesheet" href="./assets/css/main.css" /> -->
    <title>Astro Basics</title>

    <!-- Swiper CDN - 다른 CDN으로 변경 -->
    <link
      rel="stylesheet"
      href="https://unpkg.com/swiper@11/swiper-bundle.min.css"
    />
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />

    <!-- JavaScript 로딩 - 다른 CDN과 onload 이벤트 사용 -->
    <script
      src="https://unpkg.com/swiper@11/swiper-bundle.min.js"
      onload="console.log('Swiper CDN loaded successfully')"
      onerror="console.error('Failed to load Swiper CDN')"></script>
    <script type="text/javascript" src="./assets/js/common.js"></script>
    <script type="text/javascript" src="./assets/js/swiper.js"></script>
    <script type="text/javascript" src="./assets/js/tab.js"></script>
  </body>
</html>
